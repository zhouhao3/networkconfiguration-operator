
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: ports.metal3.io
spec:
  group: metal3.io
  names:
    kind: Port
    listKind: PortList
    plural: ports
    singular: port
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Port is the Schema for the ports API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PortSpec defines the desired state of Port
            properties:
              deviceRef:
                description: Reference for Device CR.
                properties:
                  kind:
                    enum:
                    - Switch
                    type: string
                  name:
                    type: string
                  namespace:
                    default: default
                    description: If empty use default namespace.
                    type: string
                required:
                - kind
                - name
                type: object
              portConfigurationRef:
                description: Reference for PortConfiguration CR.
                properties:
                  kind:
                    enum:
                    - SwitchPortConfiguration
                    type: string
                  name:
                    type: string
                  namespace:
                    default: default
                    description: If empty use default namespace.
                    type: string
                required:
                - kind
                - name
                type: object
              portID:
                description: Describes the port number on the device.
                type: string
            required:
            - deviceRef
            - portConfigurationRef
            - portID
            type: object
          status:
            description: PortStatus defines the observed state of Port
            properties:
              portConfigurationRef:
                description: The current portConfiguration of the port.
                properties:
                  kind:
                    enum:
                    - SwitchPortConfiguration
                    type: string
                  name:
                    type: string
                  namespace:
                    default: default
                    description: If empty use default namespace.
                    type: string
                required:
                - kind
                - name
                type: object
              state:
                description: The current configuration status of the port.
                type: string
            required:
            - portConfigurationRef
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []

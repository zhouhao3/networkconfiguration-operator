
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: networkbindings.metal3.io.my.domain
spec:
  group: metal3.io.my.domain
  names:
    kind: NetworkBinding
    listKind: NetworkBindingList
    plural: networkbindings
    singular: networkbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkBinding is the Schema for the networkbindings API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkBindingSpec defines the desired state of NetworkBinding
            properties:
              networkConfigurationRef:
                description: NetworkConfigurationRef is the reference for NetworkConfiguration
                  CR
                properties:
                  name:
                    type: string
                  nameSpace:
                    type: string
                required:
                - name
                - nameSpace
                type: object
              ports:
                description: If the length of ports isn't 1, it means we need do link
                  aggregation for this ports.
                items:
                  description: Port ...
                  properties:
                    deviceRef:
                      description: DeviceRef is the reference for Device CR
                      properties:
                        kind:
                          type: string
                        name:
                          type: string
                        nameSpace:
                          type: string
                      required:
                      - kind
                      - name
                      - nameSpace
                      type: object
                    portID:
                      type: string
                  required:
                  - deviceRef
                  - portID
                  type: object
                maxItems: 2
                type: array
            required:
            - networkConfigurationRef
            - ports
            type: object
          status:
            description: NetworkBindingStatus defines the observed state of NetworkBinding
            properties:
              state:
                description: StateType is the type of .status.state
                type: string
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
